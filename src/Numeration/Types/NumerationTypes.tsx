export interface NumerationTypes {
  id: number;
  authNumer: string;
  prefix: string;
  startDate: string; 
  finishDate: string;
  initialNumber: number;
  finalNumber: number;
  currentNumber: number;
  resolutionNumber: number;
  status: string;
}
